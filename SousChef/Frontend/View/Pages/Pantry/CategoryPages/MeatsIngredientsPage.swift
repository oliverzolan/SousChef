import SwiftUICore

struct MeatsIngredientsPage: View {
    var body: some View {
        BaseIngredientsPage(
            title: "Meats",
            ingredients: ["Chicken", "Bennet", "Wowzers"]
        )
    }
}
